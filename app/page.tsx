'use client';

import styled from 'styled-components';

// const Container = styled.div`
//   display: grid;
//   grid-template-columns: repeat(3, minmax(0, 1fr));
//   gap: 1.5rem /* 24px */;
// `;




export default function Page() {
  return (
    // <Container>
        <h2> Styled with Styled Components</h2>
      // </Container>
    
  );
}